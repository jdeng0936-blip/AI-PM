# 徽远成 AI-PM（智能项目经理）— 白皮书·总体设计

> **版本**：v1.0 | **日期**：2026-03-05 | **来源**：基于 Gemini 共享文档提炼

---

## 一、项目愿景与背景

### 1.1 痛点分析

传统项目管理依赖 12 列 Excel 日报，存在以下核心问题：

| 痛点 | 表现 |
|------|------|
| 录入反人类 | 员工面对 12 个格子抓耳挠腮，日报敷衍、大量留白 |
| 检查靠人工 | 主管需逐条审阅，耗时且易遗漏逻辑漏洞 |
| 监督无连续性 | 跨日任务追踪断裂，卡点无人跟进 |
| 评价主观化 | 缺乏量化标准，考核失去公信力 |
| 信息碎片化 | CEO/CTO 需翻阅大量微信消息才能了解全局 |

### 1.2 系统定位

**"徽远成 AI-PM"** 是一套以 **大模型（Gemini）** 为核心引擎的智能项目管理系统，目标是：

> **让日报录入从 5 分钟缩短到 10 秒，让管理从"人盯人"升级为"AI 驱动的自动化 PDCA 闭环"。**

### 1.3 团队优势

- 团队已深度使用大模型（张毅做 NewApi/Liberchat 接入、郭震用 Antigravity 写代码、陈家云用智能体优化仓库）
- 具备 "Eat Your Own Dog Food" 的内部验证能力
- 技术栈前沿、软硬结合，可快速自研落地

---

## 二、核心功能模块设计

系统围绕 **PDCA（Plan-Do-Check-Act）** 管理闭环，设计了 **五大核心模块**：

```
┌─────────────────────────────────────────────────────┐
│                 AI-PM 系统架构总览                     │
├─────────────────────────────────────────────────────┤
│                                                     │
│   ① 录入(Input)  ──→  ② 检查(Check)  ──→            │
│        ↑                                            │
│        │           ③ 监督(Supervise)                  │
│        │                │                            │
│        └────────────────┘                            │
│                                                     │
│   ④ 评分(Score)    ⑤ 管理(Manage)                     │
│      ↓                  ↓                            │
│   游戏化激励          战情看板 + 自动周报               │
│                                                     │
└─────────────────────────────────────────────────────┘
```

### 模块 ① — 对话式录入 (Chat-to-Form)

**核心理念**：从"填 12 个格子"变成"发一条消息/语音"

| 要素 | 设计方案 |
|------|---------|
| **交互入口** | Liberchat 客户端 / 钉钉·企微机器人 |
| **输入方式** | 自然语言文字或语音（极简输入） |
| **AI 解析引擎** | Gemini 3.1 — JSON 结构化输出 |
| **确认机制** | 系统弹出卡片 → 员工一键确认提交 |
| **效率提升** | 5 分钟 → 10 秒 |

**数据流**：

```
员工语音/文字 → Gemini 3.1 解析 → 结构化 JSON (12字段)
    → 卡片预览 → 员工一键确认 → 入库 (PostgreSQL)
```

**AI 自动填充字段示例**：

| 字段 | 自动提取示例 |
|------|------------|
| 姓名 | 张毅 |
| 今日任务 | 测试 vertex-ai 等渠道接入最新模型到 NewApi，测试接入 Liberchat |
| 完成进度 | 100% |
| 代码归档 | v2.1 |
| 验收标准 | NewApi 分发正常，Liberchat 端可正常对话（AI 自动补全） |

---

### 模块 ② — 智能质检 (Check)

**核心理念**："无情质检员" — 提交瞬间自动拦截逻辑漏洞

**质检规则**：

| 规则类型 | 检测逻辑 | 触发动作 |
|---------|---------|---------|
| **逻辑漏洞拦截** | 进度 < 100% 但【核心卡点】为空 | 打回要求补充 |
| **验收标准 SMART 审查** | 任务完成但验收标准为空 | 提示补充具体标准 |
| **规范校验** | 研发任务 100% 但无产出物（代码版本号/成果演示） | 要求补充 |

**拦截示例**：

> 👉 **张维**，进度 70%，但【核心卡点】和【解决方案】为空  
> → AI 提示：*"@张维，进度未达100%，请务必补充『核心卡点』及『预计解决时间』，以便团队了解阻碍并协调资源。"*

> 👉 **林跃文**，采购 MCU 任务，验收标准为空  
> → AI 提示：*"@跃文，采购任务不能没有验收标准哦，请补充是要求【下单完成】还是【物料入库清点完毕】？"*

---

### 模块 ③ — 跨日监督 (Supervise)

**核心理念**：AI 充当"不知疲倦的敏捷教练"，具备上下文理解能力

| 能力 | 描述 |
|------|------|
| **跨日连贯性监督** | 自动追踪前日卡点，标记为 `[待解决]`，次日重点监督推进情况 |
| **上下游依赖预警** | 识别任务间依赖关系，当上游阻塞时自动提醒下游调整节奏 |

**场景示例**：

- **跨日追踪**：陈家云 25 日提到"原材料仓缺实操"，AI 自动在 26 日监督该问题是否推进
- **依赖预警**：林跃文（采购 MCU/LoRa 模块）的进度直接影响 206 样机贴片与测试。若采购延迟，AI 自动给下游发预警

---

### 模块 ④ — 游戏化评分 (Score)

**核心理念**：多维度量化打分 + "反卷"提醒

**评分维度**：

| 维度 | 权重 | 说明 |
|------|------|------|
| 日报含金量 | 高 | 是否有实质产出描述 |
| 逻辑严密性 | 高 | 进度、卡点、方案是否自洽 |
| 规范完整度 | 中 | 必填字段是否齐全 |
| 协作贡献度 | 中 | 是否主动协调跨部门问题 |

**评分输出示例**：

```json
{
  "员工": "陈家云",
  "日期": "2月25日",
  "评分": 85,
  "等级": "良好",
  "AI点评": "工作态度积极，主动协调原材料问题，但在解决问题时效性上需在26日进一步明确节点",
  "推荐": "当日表现优秀员工",
  "管理警告": "仓库缺实操环境，可能导致后续流程阻塞，请主管郭震协助解决"
}
```

---

### 模块 ⑤ — 战情管理 (Manage)

**核心理念**：老板的"战情看板" — 每日 9:00 自动推送

| 功能 | 描述 |
|------|------|
| **项目健康度概览** | 🔴 红（卡点严重/延期）🟡 黄（依赖项异常）🟢 绿（正常推进）三色仪表盘 |
| **今日核心关注** | 全公司最紧迫的 Top 3 技术卡点 |
| **自动化周报** | 从日报中自动提炼关键里程碑 |

---

## 三、技术架构设计

### 3.1 整体架构

```
┌──────────────┐     ┌──────────────┐     ┌──────────────┐
│   前端入口    │     │   后端中枢    │     │   AI 引擎    │
│              │     │              │     │              │
│ · Liberchat  │────▶│ · FastAPI    │────▶│ · Gemini     │
│ · 钉钉/企微   │     │ · Python     │     │   1.5 Flash  │
│   机器人      │     │              │     │ · NewApi     │
│              │     │ · PostgreSQL │     │   (模型分发)   │
└──────────────┘     └──────────────┘     └──────────────┘
```

### 3.2 技术选型

| 层级 | 技术 | 说明 |
|------|------|------|
| **模型分发** | NewApi | 张毅已部署，统一管理多模型渠道 |
| **AI 模型** | Gemini 3.1 | JSON 结构化输出、响应快、成本低 |
| **后端框架** | Python FastAPI | 业务逻辑中枢，JSON 解析与路由 |
| **数据存储** | PostgreSQL | 结构化日报数据持久化 |
| **前端入口** | Liberchat 插件 / 钉钉·企微 | 复用现有聊天基础设施 |

### 3.3 核心 Prompt 工程

系统的 AI 行为由精心设计的 System Prompt 驱动：

```python
system_prompt = """
你现在是徽远成科技的"AI 项目经理"。请根据以下员工提交的单日工作记录JSON，
进行严格的检查、指出问题，并给出一个0-100分的综合评分。

【检查规则】
1. 逻辑校验：如果"完成进度"不足100%，但"核心卡点"为"无"或空，
   判定为逻辑不合格(pass_check: false)，必须扣分。
2. 规范校验：研发任务进度达到100%时，必须检查是否有产出物
  （代码版本号或成果演示）。

【返回格式要求 (必须是严格的JSON)】
{
  "pass_check": true/false,
  "reject_reason": "如果不通过，请用温和的语气指出需要员工补充什么字段",
  "score": 85,
  "ai_comment": "作为主管的一段点评（50字以内，包含肯定与建议）",
  "management_alert": "如果有进度严重滞后或需要主管协调资源，
                       请在此输出警告提取，否则为空"
}
"""
```

---

## 四、数据流全景

```
员工发送自然语言/语音
        │
        ▼
┌─────────────────┐
│  Gemini 3.1    │ ← 解析为结构化 JSON（12字段）
│  (Chat-to-Form)  │
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│  员工一键确认     │ ← 卡片预览，扫一眼确认
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│  AI 质检拦截      │ ← 逻辑漏洞检测、规范校验
│  (Check)         │     不达标 → 打回补充
└────────┬────────┘
         │ 通过
         ▼
┌─────────────────┐
│  入库 PostgreSQL  │ ← 结构化存储
└────────┬────────┘
         │
    ┌────┴────┐
    ▼         ▼
┌───────┐ ┌──────────┐
│ 评分   │ │ 跨日监督  │
│(Score) │ │(Supervise)│
└───┬───┘ └────┬─────┘
    │          │
    ▼          ▼
┌─────────────────┐
│  管理看板        │ ← 三色仪表盘 + 日报汇总 + 自动周报
│  (Manage)        │     每日 9:00 推送给 CEO/CTO
└─────────────────┘
```

---

## 五、长期研发项目跟踪（Project Lifecycle）

> 前面五大模块解决的是**日报级**的短周期管理。对于硬件研发、嵌入式开发等**跨月甚至跨季度**的长期项目，需要额外的迭代跟踪机制。

### 5.1 设计理念

日报是"每日快照"，但研发项目的生命周期远超一天。系统需要一个**纵向追踪层**，将零散的日报自动聚合为项目全景：

```
日报(Day) → Sprint(周/双周) → 里程碑(Milestone) → 项目全生命周期(Project)
```

### 5.2 核心能力

#### ① Sprint / 迭代管理

| 能力 | 描述 |
|------|------|
| **自动迭代划分** | 按周/双周自动生成 Sprint，支持自定义迭代周期 |
| **日报自动归集** | 员工日报自动按 Sprint + 所属任务归集，无需二次操作 |
| **燃尽图自动生成** | 根据每日进度数据自动绘制 Sprint 燃尽图 |
| **迭代回顾 AI 总结** | Sprint 结束时，AI 自动生成回顾报告（完成率、延期原因、改进建议） |

#### ② 里程碑 & WBS 分解

| 能力 | 描述 |
|------|------|
| **多级任务分解** | 项目 → 里程碑 → Epic → 任务（支持 WBS 层级拆解） |
| **关键路径识别** | AI 自动识别项目关键路径，标注哪些任务延期会影响整体交付 |
| **依赖链可视化** | 任务间的前置/后置依赖关系图谱，自动检测环形依赖 |

#### ③ AI 趋势分析 & 风险预测

| 能力 | 描述 |
|------|------|
| **进度趋势预测** | 根据历史 Sprint 效率，AI 预测项目整体交付时间 |
| **风险自动识别** | 连续多日进度停滞、卡点重复出现 → AI 自动升级为项目风险 |
| **资源负载均衡** | 分析各成员任务分布，识别过载或闲置，建议任务调配 |
| **技术债追踪** | 标记"临时方案"、"待重构"等技术债，定期提醒处理 |

#### ④ 多项目并行看板

| 能力 | 描述 |
|------|------|
| **项目组合视图** | 多个研发项目并行状态总览（甘特图 + 健康度） |
| **跨项目资源冲突** | 同一成员参与多个项目时，AI 检测时间冲突并预警 |
| **项目对比分析** | 对比不同项目的效率指标（Sprint 完成率、延期频率等） |

### 5.3 数据模型扩展

```
projects (项目)
  ├── milestones (里程碑)
  │     ├── epics (史诗/功能模块)
  │     │     ├── tasks (任务) ← 日报自动关联
  │     │     │     ├── daily_reports (日报记录)
  │     │     │     └── blockers (卡点记录)
  │     │     └── tech_debts (技术债)
  │     └── dependencies (依赖关系)
  ├── sprints (迭代)
  │     ├── sprint_tasks (迭代-任务关联)
  │     └── sprint_reviews (迭代回顾 - AI 生成)
  └── risk_registry (风险登记册 - AI 维护)
```

### 5.4 AI 自动化场景

| 场景 | 触发条件 | AI 动作 | 使用模型 |
|------|---------|--------|---------|
| 迭代回顾 | Sprint 结束 | 生成回顾报告 + 改进建议 | `gemini-3-flash-preview` |
| 风险升级 | 卡点连续 3 天未解决 | 升级为项目风险，通知主管 | `gemini-2.5-flash` |
| 交付预测 | 每周一早 9:00 | 基于历史效率预测交付日期 | `gemini-2.5-pro` |
| 资源告警 | 成员日任务 > 3 项 | 预警过载，建议调配 | `gemini-2.5-flash` |
| 技术债提醒 | 累积 > 5 项未处理 | 建议安排重构 Sprint | `gemini-3-flash-preview` |
| 关键路径更新 | 任何任务延期 | 重新计算关键路径并预警 | `gemini-2.5-flash` |

---

## 六、支撑体系设计

### 6.1 登录认证与安全

| 要素 | 方案 |
|------|------|
| **认证方式** | 账号密码 + JWT Token 鉴权 |
| **角色区分** | 管理员（总经理）和普通员工两种角色 |
| **会话管理** | Token 有效期 24 小时，支持记住登录 |
| **密码安全** | bcrypt 加密存储，首次登录强制改密 |
| **操作审计** | 记录登录时间、IP、关键操作日志 |

### 6.2 催报机制

未提交日报是系统最大的失效风险，需要自动催报：

| 触发时间 | 动作 | 使用模型 |
|---------|------|---------|
| 每日 17:30 | 友好提醒："今天的日报还没交哦，别忘了～" | — (系统消息) |
| 每日 20:00 | 二次催促："日报截止时间快到了，请尽快提交" | — (系统消息) |
| 每日 22:00（截止） | 标记为"未提交"，通知总经理 | `gemini-2.5-flash` (生成缺勤摘要) |
| 连续 2 天未交 | AI 自动升级预警给总经理 | `gemini-2.5-flash` |

### 6.3 通知推送渠道

| 通知类型 | 推送渠道 | 接收人 |
|---------|---------|-------|
| 日报催提醒 | 企微/钉钉机器人 | 未提交员工 |
| 质检打回 | 企微/钉钉私聊 | 被打回员工 |
| 评分结果 | 系统内通知 | 员工本人 |
| 战情日报 | 企微/钉钉 + 邮件 | 总经理 |
| 风险预警 | 企微/钉钉紧急通知 | 总经理 |
| Sprint 回顾 | 邮件 + 系统内 | 总经理 + 相关人员 |

### 6.4 岗位日报模板差异

不同岗位的工作内容差异巨大，需要按岗位定制日报模板：

| 岗位 | 特有字段 | 示例 |
|------|---------|------|
| **研发人员** | 代码版本号、技术方案、测试结果 | "v2.1 分支已通过单元测试" |
| **采购经理** | 供应商、物料名称、采购金额、到货日期 | "MCU 芯片×500 已下单，预计3天到" |
| **生产经理** | 产线编号、产量、良品率、设备状态 | "A线今日产出200台，良品率98%" |
| **仓管** | 入库/出库明细、库存预警 | "入库 MCU×500，当前库存告警：电阻不足" |
| **财务部长** | 收支明细、发票状态、预算执行率 | "本月预算执行率 72%，应收账款3笔" |
| **商务/销售** | 客户名称、跟进阶段、预计签约金额 | "XX科技 二次报价已发，预计本月签约" |
| **技术部长** | 技术评审、架构决策、技术风险 | "完成 LoRa 模块选型评审" |

AI 解析时会根据提交人的岗位自动匹配对应模板字段。

### 6.5 附件与多媒体支持

| 类型 | 使用场景 | 存储方式 |
|------|---------|---------|
| 📷 图片 | 仓管拍照验货、生产现场拍照 | 阿里云 OSS（数据库只存 URL） |
| 📄 文档 | 采购合同、技术方案 PDF | 阿里云 OSS |
| 🎤 语音 | 语音录入原始文件 | 阿里云 OSS + 讯飞 ASR 转文本 |

> 遵循架构红线 Rule 3：文件二进制严禁入库，一律存 OSS。

### 6.6 总经理 AI 对话查询

总经理可以用自然语言向 AI 提问，系统通过 RAG + SQL 实时检索回答：

| 问题示例 | AI 处理方式 | 使用模型 |
|---------|-----------|---------|
| "本周谁延期最多？" | SQL 聚合查询 → LLM 汇总 | `gemini-3-flash-preview` |
| "采购部进度怎么样？" | 按部门筛选 → LLM 分析 | `gemini-3-flash-preview` |
| "206 项目的关键风险是什么？" | RAG 检索 + 风险表 → LLM 推理 | `gemini-2.5-pro` |
| "帮我生成本周的管理周报" | 全员日报聚合 → LLM 撰写 | `gemini-2.5-pro` |

### 6.7 历史趋势看板

| 维度 | 展示内容 |
|------|---------|
| **个人趋势** | 某员工近 30 天评分曲线、提交准时率 |
| **部门对比** | 各部门平均评分、提交率对比柱状图 |
| **项目趋势** | 项目健康度随时间变化（红黄绿比例） |
| **效率指标** | Sprint 完成率趋势、任务平均完成天数 |

### 6.8 数据导出

| 导出格式 | 使用场景 |
|---------|---------|
| **Excel** | 总经理月度汇报、财务对账 |
| **PDF** | 项目阶段总结报告、审计存档 |
| **CSV** | 数据分析师进一步分析 |

支持按时间范围、人员、部门、项目筛选导出。

### 6.9 KPI 目标设定

评分系统需要有基准锚点，否则打分没有意义：

| 设定维度 | 示例 |
|---------|------|
| **日报提交率** | 目标 ≥ 95%（每月允许 1 天未交） |
| **平均评分** | 目标 ≥ 75 分 |
| **任务完成率** | 本 Sprint 目标 ≥ 80% |
| **卡点解决时效** | 卡点提出后 ≤ 3 天必须有推进 |

总经理可以为全公司或按岗位设定不同的 KPI 目标。

### 6.10 部门与项目分组

虽然所有人直接向总经理汇报，但查看时需要分组筛选：

| 分组方式 | 说明 |
|---------|------|
| **按部门** | 技术组、生产组、采购组、商务组、财务等 |
| **按项目** | 206 样机项目、LoRa 模块项目、仓库智能化项目等 |
| **按角色** | 研发人员、部门负责人等 |

总经理可以一键切换视图：全员 / 按部门 / 按项目。

### 6.11 移动端适配

| 场景 | 方案 |
|------|------|
| **日报录入** | 响应式布局，手机浏览器可用；语音录入对手机友好 |
| **总经理查看** | 战情看板适配手机端，支持左右滑动切换 |
| **推送通知** | 企微/钉钉原生推送，点击直接跳转系统 |

### 6.12 知识沉淀与复用

| 能力 | 描述 |
|------|------|
| **自动提取经验** | 日报中的解决方案自动标记并存入知识库 |
| **相似问题推荐** | 员工遇到卡点时，AI 推荐历史上类似问题的解决方案 |
| **最佳实践沉淀** | 高评分日报中的方法论自动归档为团队最佳实践 |

### 6.13 请假与出差状态

| 状态 | 系统行为 |
|------|---------|
| **正常上班** | 正常催报，未交标记缺勤 |
| **请假** | 不催报，不计入提交率考核 |
| **出差** | 允许简化日报（仅填出差事由和进展） |
| **节假日** | 自动跳过，不催报 |

员工可提前在系统中标记状态，或由总经理统一设置。

### 6.14 考勤系统联动

| 对接方式 | 说明 |
|---------|------|
| **企微/钉钉考勤** | 自动同步打卡状态，识别出勤/请假/出差 |
| **手动标记** | 未对接考勤系统时，支持手动设置状态 |

---

## 七、MVP 实施路线

### 7.1 分阶段交付

| 阶段 | 周期 | 交付内容 |
|------|------|---------|
| **MVP** | 3 天 | Chat-to-Form 解析 + 一键确认 + 基础质检 |
| **V1.0** | 2 周 | 完整质检规则 + 评分系统 + 跨日监督 |
| **V2.0** | 1 个月 | 战情看板 + 自动周报 + 上下游预警 + 权力地图 |

### 7.2 系统角色设计

所有人员向总经理汇报，统一通过系统录入日报：

| 角色 | 说明 | 系统操作 |
|------|------|---------|
| **总经理** | 管理者 | 查看所有人日报、评分、战情看板、项目全局跟踪 |
| **财务部长** | 日报提交人 | 录入日报 |
| **采购经理** | 日报提交人 | 录入日报 |
| **技术部长** | 日报提交人 | 录入日报 |
| **生产经理** | 日报提交人 | 录入日报 |
| **商务经理** | 日报提交人 | 录入日报 |
| **研发人员（多人）** | 日报提交人 | 录入日报 |
| **销售部长** | 日报提交人 | 录入日报 |
| **仓管** | 日报提交人 | 录入日报 |

---

## 八、总结

**AI-PM 系统的核心价值**：

1. ✅ **效率革命** — 日报录入从 5 分钟 → 10 秒
2. ✅ **质量把关** — AI 自动质检，杜绝敷衍和逻辑漏洞
3. ✅ **持续监督** — 跨日追踪卡点，上下游依赖预警
4. ✅ **量化管理** — 多维度评分，让考核有据可依
5. ✅ **全局视野** — 战情看板让管理层一目了然

> **一句话总结：用 AI 将"每日苦不堪言的填表 + 主管逐条审阅"转变为"说句话就完事 + AI 全自动 PDCA 闭环管理"。**

---
*本白皮书基于 Gemini 共享文档 ([来源链接](https://gemini.google.com/share/152eb7e6b1ae)) 提炼整理。*
